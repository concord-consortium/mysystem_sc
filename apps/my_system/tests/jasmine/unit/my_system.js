/*globals MySystem describe it expect xit xdescribe beforeEach afterEach spyOn runs waits waitsFor */

describe("MySystem", function () {
  it("should be defined", function () {
    expect(MySystem).toBeDefined();
  });
});
